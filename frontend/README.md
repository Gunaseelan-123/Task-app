# Northstar Frontend

Next.js 15 + Tailwind CSS storefront and admin shell.

## Run
1. `cd frontend`
2. `npm install`
3. `npm run dev`

Set `NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1` to connect to the Laravel backend.
